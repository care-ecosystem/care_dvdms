def save_indent(institute, record_order, user):
    """Save (create) an indent in DVDMS for a record order."""


def track_indent(institute, outward_record, user):
    """Track an indent's status in DVDMS for an outward record."""
